apiVersion: apps/v1
kind: Deployment
metadata:
  name: driverservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: driverservice
  template:
    metadata:
      labels:
        app: driverservice
    spec:
      # POD SECURITY CONTEXT
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      
      containers:
      - name: driverservice
        image: acruitgoprod.azurecr.io/driverservice:latest
        ports:
        - containerPort: 8000
        
        # CONTAINER SECURITY CONTEXT  
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        
        # RESOURCE LIMITS
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        
        # VOLUME MOUNTS
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        
        envFrom:
        - secretRef:
            name: uitgo-secrets
        env:
        - name: MONGODB_URL
          valueFrom: { secretKeyRef: { name: uitgo-secrets, key: COSMOS_CONNECTION_STRING } }
      
      # VOLUMES
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: driverservice
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: driverservice